[user]
    name = lime
    email = 114978689+lime-desu@users.noreply.github.com

[init]
    defaultBranch = main

[color]
    ui = auto

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red
    new = green 
    
[color "status"]
    added = yellow
    changed = green
    untracked = red

[alias]
    update = "!git pull --recurse-submodules && git submodule update --remote"

[pull]
    rebase = true

[core]
    pager = delta
    excludesfile = $HOME/.config/git/gitignore

[gc]
    reflogExpire = 200
    reflogExpireUnreachable = 90

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    light = false
    line-numbers = true
    line-numbers-minus-style = red
    line-numbers-plus-style = green
    line-numbers-zero-style = black
    file-style = blue bold
    plus-style = green
    minus-style = red
    plus-emph-style = syntax "#40a02b"  # green latte
    minus-emph-style = syntax "#d20f39" # red latte

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[help]
    autocorrect = 1
